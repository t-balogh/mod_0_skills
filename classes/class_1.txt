Class - Restaurant

Attributes:
table_numbers(array)
chairs(integer)
smoking_section(boolean)
menus(integer)

Methods:
adding_table(adds add_table to table_numbers in array)
rearranging_tables(rearranging in the array)
removing_chair(subtracts remove_chair to chairs)
printing_new_menus(subtracts current menus and adds new menus)
